export type expense = {
    value: string,
    description: string,
}

export type ExpenseModel = {
    id: number,
    value: string,
    description: string,
    created_at: string,
    updated_at: string,
}